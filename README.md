# TO-calculate-are-of-square-in-C-language-
I created just a area calculater of square from C language <br>

#include <stdio.h> <br>
// To calculate area of a square<br>
int main() { <br>
    int side ;<br>
    printf ("enter side");<br>
    scanf ("%d", &side);<br>
    printf("area is:%d", side * side);<br>
    return 0;<br>
}